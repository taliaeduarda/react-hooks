import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'
import AppComplement from './AppComplement';

ReactDOM.render(
  <React.StrictMode>
    <AppComplement />
  </React.StrictMode>,
  document.getElementById('root')
);

